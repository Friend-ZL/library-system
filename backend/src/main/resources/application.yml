server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://124.222.215.96:13306/library_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

app:
  jwt:
    secret: library-system-secret-key-change-me-please-use-at-least-64-bytes
    expiration-minutes: 720
  borrow:
    default-days: 30
    max-active-count: 5
